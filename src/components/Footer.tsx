import Link from 'next/link';

export default function Header() {
	return (
		<div className="flex gap-4 w-full flex-col mb-4">
			<p className="">
				<i>
					&quot;The road is never straight. Growth is a spiral process, doubling back on itself, reasessing and
					regrouping.&quot;
				</i>
			</p>
		</div>
	);
}
